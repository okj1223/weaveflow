# Examples

This directory is for curated examples that are safe to keep in Git.

Generated dogfooding result artifacts such as `*_codex_result.md` are local
workflow outputs and are ignored by default. If a generated artifact contains a
useful pattern, copy or summarize the reusable part into a reviewed example
file with a clearer name.
